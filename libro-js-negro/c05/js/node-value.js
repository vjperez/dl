var itemTwo = document.getElementById('two');  // Get second list itemvar elText  = itemTwo.firstChild.nodeValue;    // Get its text contentelText = elText.replace('pine nuts', 'kale');  // Change pine nuts to kaleitemTwo.firstChild.nodeValue = elText;         // Update the list item// lets do another onedocument.getElementById('four').firstChild.nodeValue = 'victor perez';document.getElementById('five').firstChild.nodeValue = 'cambie el cuarto item sin leer el valor anterior del texto y sin usar String.replace.  El p con id=5 tiene un text node child creado por el white space en HTML, una vez accedes completamente este nodo de texto entonces puedes usar firstChild.nodeValue. ';document.getElementById('six').innerText = 'node value es solo para el texto de text nodes ya existentes. El elemento six, NO tiene un text node child pq no tiene white spaces en HTML, so no puedes usar firstChild.nodeValue en la div six. Puedes usar textContent, innerText o innerHTML ya que estos no bregan con el texto de un nodo de texto especifico. Estos reescriben y recrean todo el contained text or markup incluyendo los nodos necesarios.<br>';