// Store the first list item in a variablevar firstItem = document.getElementById('one');// Get the content of the first list itemvar itemContent = firstItem.innerHTML;// Update the content of the first list item so it is a linkfirstItem.innerHTML = '<a href=\"http://example.org\">' + itemContent + '</a>';document.getElementById('scriptResults').innerHTML = '<p>Nunca uses innerHTML para insertar html que viene de usuarios, hay caracteres q si no los escapas le das poder al usuario a q afecte la pagina.<br>Te expones a cross site scripting attacks con una simple coma mal puesta. Cuando el contenido viene del usuario mejor crea nodos usando DOM manipulation. </p>';