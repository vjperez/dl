var thirdItem = document.getElementById('three'); // Find first list itemthirdItem.textContent = 'sourdough bread';       // Update the first list itemvar firstItem = document.getElementById('one'); // Find first list itemvar showTextContent = firstItem.textContent;    // Get value of textContentvar showInnerText = firstItem.innerText;        // Get value of innerText// Show the content of these two properties at the end of the listvar msg = '<p>textContent: ' + showTextContent + '</p>';    msg += '<p>innerText: ' + showInnerText + '</p>';	msg += '<p>el css de esta pagina esconde los elementos em, innerText es sensible a esto, por eso es mas lento. textContent no es sensible a css. Si quitas el style de el html ambos innerText y textContent lucen iguales.</p>';    msg += '<p><br>textContent es mas Firefox supported y mas standard, innerText es mas IE supported.</p>';	var el = document.getElementById('scriptResults');el.innerHTML = msg;