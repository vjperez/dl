<!doctype html>
<html>
    <head>
        <title>bordes</title>
        <meta charset="utf-8">
        <style>
          .separado{ margin:20px; }
          .bordeado{ border:4px solid #ffcc00 }
          .tamano{ width:100px; height:100px }
          #blue{background:blue; border-radius:20px 30px 5px 50px}
          #green{ background:green; border-radius:50px }
          #red{ background:red; border-top-left-radius:50px; border-bottom-right-radius:50px}
          
          #yellow{ background:yellow; border-top-left-radius:50px 20px; border-top-right-radius:50% 20% }
        </style>
    </head>
    <body>
        
        <!-- google border-style  border-color  border-width 
        To use border color y border width nesecitas primero 
        especificar border style
        
        tambien se pueden especificar cada lado aparte siguiendo el orden
        top-left top-right bottom-right bottom-left "casi" como en margin (see the blue one), o 
        solo una a la vez,  see the red one
        see w3schools just google it
        -->
        <div id="blue" class="bordeado tamano separado">victor</div>
        <div id="green" class="bordeado tamano separado">krystal</div>
        <div id="red" class="bordeado tamano separado">luis</div>
        
        <!-- si especificas una de las cuatro esquinas y le das 2 valores
        te da un  efecto de ellipse. see yellow one-->
        <div id="yellow" class="bordeado tamano separado">diego</div>
    </body>    
</html>
